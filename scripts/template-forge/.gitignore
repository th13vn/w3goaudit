# template-forge is itself under the repo-level gitignored scripts/, but keep a
# local ignore so the folder is self-contained if ever copied elsewhere.
.env
node_modules/
cache/
state/
candidates/
reports/
knowledge/
*.log
